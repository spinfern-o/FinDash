{
  "name": "findash",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Serverless dependencies for the FinDash column-mapping endpoint. The dashboard page itself has no runtime dependencies.",
  "dependencies": {
    "@anthropic-ai/sdk": "^0.128.0"
  }
}
